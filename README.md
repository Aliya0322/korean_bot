# Korean Language Learning Bot 🇰🇷

[![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)](https://python.org)
[![aiogram](https://img.shields.io/badge/aiogram-3.x-blue.svg)](https://docs.aiogram.dev/)
[![Mistral AI](https://img.shields.io/badge/Mistral_AI-Codestral-orange.svg)](https://mistral.ai)
[![Pollination](https://img.shields.io/badge/Pollination-Flux-green.svg)](https://pollinations.ai)

Telegram-бот для изучения корейского языка с использованием искусственного интеллекта. Бот помогает пользователям изучать корейский язык через различные функции: проверку орфографии, генерацию текстов и ежедневную рассылку слов с автоматической генерацией изображений и примеров использования.

## ✨ Основные функции

- **Проверка орфографии** 🖍 - исправление ошибок в корейских текстах с объяснениями
- **Генерация текстов** 📩 - создание текстов на корейском языке в различных стилях (формальный, неформальный, дружеский)
- **Ежедневная рассылка** 📚 - автоматическая отправка слов дня для подготовки к TOPIK 1 с:
  - Корейским словом и переводом
  - Примером использования (генерируется AI)
  - Изображением слова (генерируется AI моделью Flux)
- **Обратная связь** 🧡 - система сообщений между пользователями и администратором
- **Лимит запросов** ⚙️ - ограничение количества запросов к ИИ в день (настраивается)

## 🛠 Технологии

- **Python 3.8+** - основной язык программирования
- **aiogram 3.4.1** - асинхронная библиотека для работы с Telegram Bot API
- **Mistral AI (Codestral)** - ИИ для обработки текстов, генерации примеров и переводов
- **Pollination API (Flux)** - генерация реалистичных изображений для слов
- **SQLite** - база данных для хранения пользователей
- **APScheduler** - планировщик задач для ежедневной рассылки
- **python-dotenv** - управление переменными окружения
- **Requests & Pillow** - работа с HTTP запросами и обработка изображений

## Установка и настройка

### 1. Клонирование репозитория

```bash
git clone https://github.com/Aliya0322/korean_bot.git
cd korean_bot
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корневой директории проекта на основе примера:

```env
# Telegram Bot Configuration
BOT_TOKEN=your_telegram_bot_token
ADMIN_ID=your_admin_telegram_id

# Mistral AI Configuration
API_KEY=your_mistral_api_key
MODEL_NAME=codestral-latest

# Database Configuration (опционально для PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/korean_bot

# Application Settings
MAX_REQUESTS_PER_DAY=10
```

**Где получить:**
- `BOT_TOKEN`: создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
- `API_KEY`: получите API ключ на [Mistral AI](https://mistral.ai)
- `ADMIN_ID`: ваш Telegram ID (можно узнать у [@userinfobot](https://t.me/userinfobot))

### 4. Подготовка базы данных

База данных SQLite (`korean_bot.db`) создается автоматически при первом запуске. Таблица `users` также создается автоматически.

Для использования PostgreSQL (опционально):
1. Установите PostgreSQL
2. Создайте базу данных согласно `DATABASE_URL`
3. Обновите код в `db.py` для использования PostgreSQL вместо SQLite

### 5. Подготовка словаря

Файл `korean_english_pairs.txt` уже включен в репозиторий. Формат файла:
```
1 그리고 - And
3 그래서 - So/therefore
4 막혀요 - Traffic jam
```

Каждая строка содержит:
- Номер (опционально)
- Корейское слово
- Английский перевод (разделитель: ` - `)

### 6. Запуск бота

```bash
python Telegram_Korean.py
```

## 📁 Структура проекта

```
korean_bot/
├── Telegram_Korean.py       # Основной файл бота с обработчиками команд
├── configuration.py          # Конфигурация и переменные окружения
├── db.py                     # Работа с базой данных SQLite
├── scheduler.py              # Планировщик ежедневной рассылки
├── image_generator.py        # Генератор изображений через Pollination API
├── korean_english_pairs.txt  # Словарь корейских слов (EN перевод)
├── temp_images/              # Временные изображения (автоматически)
├── images/                   # Старые изображения (не используется)
├── requirements.txt          # Зависимости Python
├── LICENSE                   # MIT License
├── .env                      # Переменные окружения (не в git)
├── .gitignore                # Игнорируемые файлы
└── README.md                 # Документация
```

## Функциональность

### Проверка орфографии

Пользователь отправляет текст на корейском языке, а ИИ:
- Исправляет ошибки
- Объясняет изменения на русском языке
- Сохраняет стиль и структуру текста

### Генерация текстов

Интерактивный процесс создания текстов:
1. Пользователь описывает тему текста
2. Выбирает стиль (формальный/неформальный/дружеский)
3. Может добавить дополнительные пожелания
4. ИИ генерирует текст на корейском языке

### Ежедневная рассылка

Автоматическая отправка слов дня включает:

1. **Выбор слова**: случайный выбор из `korean_english_pairs.txt`
2. **Перевод**: автоматический перевод с английского на русский (Mistral AI)
3. **Пример использования**: генерация примера на корейском языке (Mistral AI)
4. **Изображение**: генерация реалистичного изображения (Pollination API + Flux)
   - Использует английский перевод для лучшего качества
   - Учитывает корейский культурный контекст
   - Не содержит людей и религиозных тем

**Время отправки**: по умолчанию в 9:00 утра каждый день

### Система обратной связи

- Пользователи отправляют сообщения администратору
- Администратор получает уведомления с кнопкой "Ответить"
- Ответы отправляются напрямую пользователю
- Все действия логируются

## ⚙️ Настройка

### Лимит запросов

Измените значение `MAX_REQUESTS_PER_DAY` в `.env` файле:

```env
MAX_REQUESTS_PER_DAY=10  # По умолчанию 10 запросов в день на пользователя
```

### Время рассылки

Измените параметры в `Telegram_Korean.py`:

```python
# Для тестового режима (каждую минуту)
schedule_daily_word(test_mode=True)

# Для продакшн режима (9:00 утра)
schedule_daily_word(test_mode=False, hour=9, minute=0)
```

### База слов

Добавьте новые слова в `korean_english_pairs.txt`:

**Формат:**
```
[номер] [корейское_слово] - [английский_перевод]
```

**Пример:**
```
961 새로운 - new
962 친구 - friend
```

**Примечания:**
- Номер опционален
- Разделитель: ` - ` (дефис с пробелами)
- Комментарии (`#`) и пустые строки игнорируются
- Для нескольких вариантов перевода используйте `/`: `So/therefore`

### Стили генерации

Настройте стили в `configuration.py`:

```python
TEXT_STYLE = "Дружелюбный и информативный тон"
IMAGE_STYLE = "Реалистичные изображения, безопасный контент"
```

## 🔒 Безопасность

- ✅ Все секретные данные хранятся в `.env` файле
- ✅ Файл `.env` добавлен в `.gitignore`
- ✅ Используется лимит запросов для предотвращения злоупотреблений
- ✅ Валидация входных данных (максимальная длина текста: 200 символов)
- ✅ Обработка ошибок и исключений

## 📝 Логирование

Бот ведет подробные логи всех операций:

- 🎨 Процесс генерации изображений
- 🌐 Запросы к внешним API
- ✅ Успешные отправки сообщений
- ❌ Ошибки отправки сообщений
- 👤 Действия администратора
- 🗄️ Ошибки работы с базой данных

##  Решение проблем

### Бот не отправляет сообщения

1. Проверьте, что `BOT_TOKEN` правильный
2. Убедитесь, что бот запущен
3. Проверьте логи на наличие ошибок

### Изображения не генерируются

1. Проверьте интернет-соединение
2. Pollination API может быть временно недоступен
3. При ошибке бот отправит сообщение без изображения
4. Проверьте логи: `🎨 Генерация изображения...`

### Ошибки с базой данных

1. Убедитесь, что есть права на создание файлов
2. Проверьте путь к базе данных
3. При необходимости удалите `korean_bot.db` для пересоздания


## Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для получения дополнительной информации.
